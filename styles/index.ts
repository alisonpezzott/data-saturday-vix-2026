// Global styles entry — Slidev loads `styles/index.ts` automatically.
// Fonts are bundled from npm so the deck renders identically offline on stage;
// nothing here reaches out to Google Fonts.
import '@fontsource-variable/geist'
import '@fontsource-variable/geist-mono'
import './theme.css'
import './infographics.css'
